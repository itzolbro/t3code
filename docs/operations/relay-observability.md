# Relay observability — REMOVED

> **Historical.** This document is retained only as an upstream reference.

The T3 Connect relay and its Alchemy stack (`infra/relay`) were deleted in the
Pi Tie fork (Phase 1: mobile/marketing/relay/experiments trim). Relay
observability surfaces — the shared Axiom trace datasets, scoped ingest tokens,
and `relay.operation` span annotations — no longer exist in this repository.

See `docs/operations/observability.md` for the observability surfaces that
remain.
