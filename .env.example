# Pi Tie — local-only build
# No cloud features: no Clerk auth, no T3 Connect relay, no Tailscale sharing.
# The desktop app spawns the local server with a bearer token generated at
# spawn time; nothing here is required for a normal local run.

